<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.stockapp.repository.StockRepository">
  <select id="findStocks" resultType="com.example.stockapp.entity.Stock">
    SELECT id, name, code, price, delete_flag
    FROM stock
    WHERE delete_flag = 0
  </select>
  

  <select id="findById" parameterType="int" resultType="com.example.stockapp.entity.Stock">
    SELECT id, name, code, price, delete_flag
    FROM stock
    WHERE id = #{id} AND delete_flag = 0
  </select>

  <insert id="insertStock" parameterType="com.example.stockapp.entity.Stock">
    INSERT INTO stock (name, code, price, delete_flag)
    VALUES (#{name}, #{code}, #{price}, #{deleteFlag})
  </insert>

  <update id="updateStock" parameterType="com.example.stockapp.entity.Stock">
    UPDATE stock
    SET name = #{name}, code = #{code}, price = #{price}
    WHERE id = #{id}
  </update>

  <delete id="deleteStock" parameterType="int">
    UPDATE stock SET delete_flag = 1 WHERE id = #{id}
  </delete>

  <select id="countByCode" parameterType="string" resultType="int">
    SELECT COUNT(*) FROM stock WHERE code = #{code} AND delete_flag = 0
  </select>
</mapper>